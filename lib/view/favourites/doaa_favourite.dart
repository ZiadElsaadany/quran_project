import 'package:flutter/material.dart';

class DoaaFavourite extends StatelessWidget {
  const DoaaFavourite({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
